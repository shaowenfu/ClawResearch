# Moltbook MBC-20 深度调研报告 (V2)

**日期**: 2026-02-13
**版本**: V2 (Orchestrated)
**来源**: 代码审计 + Gemini Context + 市场数据

---

## 1. 核心概念：什么是 MBC-20？

MBC-20 是建立在 **Moltbook**（AI Agent 社交网络）之上的代币协议。它模仿了比特币生态的 **BRC-20** 协议，通过在社交媒体帖子中嵌入特定的 JSON 数据（铭文）来发行和铸造代币。

### 核心差异：Layer 2.5 架构
与传统的 ERC-20 直接在链上发行不同，MBC-20 采用了一种 **“链下索引，链上结算”** 的混合架构：

1.  **社交层（共识层）**：Agent 在 Moltbook 发帖 `mbc-20 mint`。这是“挖矿”过程（Proof of Post）。
2.  **索引层（验证层）**：开源索引器（Indexer）扫描 Moltbook 帖子，根据时间戳和规则验证有效性，记账。
3.  **结算层（资产层）**：经过验证的余额可以通过 `MBC20Factory` 桥接到 **Base Mainnet**，成为标准的 ERC-20 代币。

---

## 2. 技术深度审计 (基于 Solidity 源码)

通过审计 `MBC20Token.sol` 核心合约，我们发现其代币经济学被硬编码在合约层，具有强制性。

### 2.1 交易税机制 (Fee Structure)
MBC-20 标准代币强制执行 **2% 的交易税**（针对 DEX 交易，点对点转账免费）。税收分配如下：

*   🔥 **1.0% 销毁 (Burn)**：通缩机制，持续减少供应量。
*   🛠️ **0.5% 团队 (Team)**：流向协议开发者。
*   🎁 **0.5% 奖励池 (Reward)**：用于未来的生态激励。

### 2.2 “销毁即权益” (Burn-to-Earn Discount)
这是该协议最独特的设计。用户可以通过主动销毁代币来永久豁免交易税。合约中硬编码了以下阶梯：

| 累计销毁量 (Burned) | 费率折扣 | 实际费率 |
| :--- | :--- | :--- |
| 100 Tokens | 0.1% off | 1.9% |
| 1,000 Tokens | 1.0% off | 1.0% |
| **10,000 Tokens** | **2.0% off** | **0% (免税)** |

**洞察**：这创造了一个博弈论模型。高频交易者（如做市 Agent）有极大的动力在早期销毁 10,000 个代币以获取终身免税权，这锁定了早期流动性并加速通缩。

---

## 3. 市场机会与 V2 展望

### V1 vs V2
*   **V1 (现状)**：部署权限受限（Admin Only），主要资产是 $CLAW。
*   **V2 (未来)**：**Permissionless (无许可)**。任何 Agent 都可以通过燃烧 $CLAW 来部署自己的 MBC-20 代币。

### 最大的 Alpha：Deployer Fee
在 V2 协议中，**代币部署者将获得 1% 的终身交易税分成**。
这意味着，如果你开发了一个被广泛使用的 Agent 工具（如“预测市场 Agent”），并发行为 MBC-20 代币，你将从所有二级市场交易中抽取 1% 的被动收入。这为 Agent 提供了**原生的商业模式**。

---

## 4. 结论与行动建议

1.  **对于交易者**：关注 $CLAW 的销毁数据。随着 V2 上线，部署需求会消耗大量 $CLAW。
2.  **对于开发者 (我们)**：
    *   **机会**：开发一个 **"One-Click Deployer" Agent**。帮助非技术用户/Agent 一键发行 MBC-20 V2 代币。
    *   **套利**：监控 Moltbook 索引器与 Base 链上余额的延迟，可能存在跨层套利机会。

> *数据来源：Github (floflo777/mbc20), mbc20.xyz, Gemini Analysis.*
